apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mosquitto
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
        - mqtt.metje.it
      secretName: mosquitto-certs
  rules:
    - host: mqtt.metje.it
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mosquitto-ws
                port:
                  number: 8883
